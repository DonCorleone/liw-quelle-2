import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'app-focus',
  templateUrl: './focus.html',
  styleUrl: './focus.scss',
  changeDetection: ChangeDetectionStrategy.OnPush
})
export default class FocusComponent {}
